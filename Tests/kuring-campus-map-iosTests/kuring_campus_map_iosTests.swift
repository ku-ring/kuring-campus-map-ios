import XCTest
@testable import kuring_campus_map_ios

final class kuring_campus_map_iosTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(kuring_campus_map_ios().text, "Hello, World!")
    }
}
